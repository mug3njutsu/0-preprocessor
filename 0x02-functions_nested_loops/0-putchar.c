#include <main.h>

/**
 * main - prints "_putchar"
 *
 * Return: Always 0 -> (Success)
 */

int main(void)
{
	#include <main.h>
	int i;

	for (i < 0; i < (sizeof(out)); i++)
	{
		putchar(out[i]);
	}

	return (0);
}
