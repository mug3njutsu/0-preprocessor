#include "main.h" /** custom header file */

/**
 * main - prints "_putchar"
 *
 * Return: Always 0 -> (Success)
 */

int main(void)
{
	char out[] = "_putchar";
	int a;

	for (a < 0; a < (sizeof(out)); a++)
	{
		_putchar(out[i]);
	}

	return (0);
}
