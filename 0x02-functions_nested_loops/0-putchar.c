#include "main.h" /** Custom header file */

/**
 * main - print "_putchar"
 *
 * Return: Always 0 -> (Success)
 */

int main(void)
{

	char out[] = "_putchar";

	for (a = 0; a < (sizeof(out)); a++)
	{
		_putchar(out[a])
	}
	return (0);
}
